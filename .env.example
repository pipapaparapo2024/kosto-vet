# Backend API (FastAPI). Leave empty in local to use Vite proxy → localhost:8000
# Production example: https://api.kosto-vet.ru
VITE_API_BASE_URL=

# Optional override when VITE_API_BASE_URL is empty (Vite proxy target)
# VITE_API_PROXY_TARGET=http://localhost:8000
